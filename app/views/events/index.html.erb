<!-- banner -->
	<%= render 'banner' %>
	<!-- //banner -->
	<!-- banner-bottom -->
    <%= render 'banner_bottom' %>
	<!-- //banner-bottom -->
	<!-- _event_of_the_month -->
	<%= render 'event_of_the_month' %>
	<!-- //_event_of_the_month -->
	<!-- posts -->
	<%= render 'recent_events' %>
	<!-- //posts -->
	<!-- services -->
	<%= render 'upcoming_events' %>
	<!-- //services -->
	
	<!-- footer -->	
<!-- //here ends scrolling icon -->