<div class="header">
			<div class="container">
				<div class="header-left">
					<div class="w3layouts-logo">
						<h1>
							<a href="<%= root_url %>">Events <span>PK</span></a>
						</h1>
					</div>
				</div>
				<div class="top-nav">
					<nav class="navbar navbar-default">
							<div class="navbar-header">
								<button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
									<span class="sr-only">Toggle navigation</span>
									<span class="icon-bar"></span>
									<span class="icon-bar"></span>
									<span class="icon-bar"></span>
								</button>
							</div>
						<!-- Collect the nav links, forms, and other content for toggling -->
						<div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
							<ul class="nav navbar-nav">
								<li><a  href="<%= root_url %>" class="<%= class_active('',root_url) %>">Home</a></li>
							    
							   <li><a href="/events" class="<%= class_active('/events') %>">Events</a></li>				
								<li><a href="/gallery" class="<%= class_active('/gallery') %>">Gallery</a></li>
								<li><a href="/contact" class="<%= class_active('/contact') %>">Contact</a></li>
								<% if current_user %>
								<li><a href="<%= new_event_path %>" class="<%= class_active(new_event_path) %>">Add Event</a></li>
								<% end %>
								<% if current_user %>
                                <li><%= link_to "Sign Out", session_path, method: :delete %></li>
								<% else %>
								<li><a href="<%= new_session_path %>" class="<%= class_active(new_session_path) %>">Sign In</a></li>
								<li><a href="<%= new_user_path %>"  class="<%= class_active(new_user_path) %>">Sign Up</a></li>
								<% end %>
							</ul>	
							<div class="clearfix"> </div>
						</div>	
					</nav>		
				</div>
				<div class="clearfix"> </div>
			</div>
		</div>